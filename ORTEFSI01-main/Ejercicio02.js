//Dado un array, multiplicar todos sus valores por el que se encuentra en la posición anterior, 
//menos al siguiente, sin modificar el array original

const data = [2, 3, 5, 12, 54, 5, -1, 0, 4, 23, 66, 7];
let numerosOrdenados = [];

for (let i = 0; i < numeros.length; i++){
    const element = numeros [i];

    numerosOrdenados.sort(element);
}